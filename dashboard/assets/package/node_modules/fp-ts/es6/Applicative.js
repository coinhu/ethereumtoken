import { getFunctorComposition } from './Functor';
export function getApplicativeComposition(F, G) {
    return Object.assign(Object.assign({}, getFunctorComposition(F, G)), { of: a => F.of(G.of(a)), ap: (fgab, fga) => F.ap(F.map(fgab, h => (ga) => G.ap(h, ga)), fga) });
}
